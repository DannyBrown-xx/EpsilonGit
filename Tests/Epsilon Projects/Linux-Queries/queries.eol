"Git Repository Queries".println();
"======================\n\n".print();

"COMMIT INFORMATION".println();
"Number of Commits: ".print(); RevCommit.all.size().println();
if(RevCommit.all.size() > 0) {
	"Commit log:".println();
	for(commit in RevCommit.all) {
		("* " + commit.getFullMessage()).println();
	}
	"\n\n".print();
}

"TAG INFORMATION".println();
"Number of Tags: ".print(); RevTag.all.size().println();
if(RevTag.all.size() > 0) {
	"Tag Information:".println();
	for(tag in RevTag.all) {
		("* " + tag.getTagName()).println();
	}
}